using DeckPrint;
using System.Collections;

// var files = new List<string>(); 
// string sdira= "DeckFolder"; 
// foreach (string d in Directory.GetDirectories(sdira)) {   
//     files.AddRange(Directory.GetFiles(d, "*.*"));   
// }

// // To use
// foreach(string file in files) { }
// string file2 = files[2]; 
// // or convert to array
// string[] decks = files.ToArray();

Console.WriteLine("Please type Create if you want to make a deck if you want to view made decks type anything else.");
String Choice = Console.ReadLine();
Console.WriteLine(string.Format($"{Choice}"));

    // if(Choice == ("Create"))
    // {

    // }
    // if(sdira == "DeckFolder")
    // {
    // Printer loreHold = new Printer();
    // loreHold.PrintDeck();
    //}